<%= form_for(@arct_edit) do |f| %>
  <% if @arct_edit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arct_edit.errors.count, "error") %> prohibited this arct_edit from being saved:</h2>

      <ul>
      <% @arct_edit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
